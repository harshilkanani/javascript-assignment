const str = "This is a JavaScript script";
const word = "script";

if (str.includes(word)) {
  console.log(`The string contains the word "${word}"`);
} else {
  console.log(`The string does not contain the word "${word}"`);
}